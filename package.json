{
  "name": "contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx hardhat compile",
    "deploy_staking": "npx hardhat run --network testnet ./scripts/deploy/deploy_staking.js",
    "deploy_nft": "npx hardhat run --network testnet ./scripts/deploy/deploy_nft.js",
    "deploy_voting": "npx hardhat run --network testnet ./scripts/deploy/deploy_voting.js",
    "stake": "npx hardhat run --network testnet ./scripts/stake.js",
    "mint_nft": "npx hardhat run --network testnet ./scripts/mint-nft.js",
    "vote": "npx hardhat run --network testnet ./scripts/vote.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.3",
    "ethers": "^5.5.2",
    "hardhat": "^2.7.0"
  },
  "dependencies": {
    "@alch/alchemy-web3": "^1.1.9",
    "@openzeppelin/contracts": "^4.4.0",
    "dotenv": "^10.0.0",
    "web3": "^1.6.1"
  }
}